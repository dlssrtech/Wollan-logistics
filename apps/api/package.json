{
  "name": "@homesphere/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node dist/main.js",
    "start:dev": "tsc --watch --preserveWatchOutput -p tsconfig.json",
    "start:prod": "node dist/main.js",
    "build": "nest build",
    "smoke-test": "node scripts/smoke-test.mjs",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "jest --passWithNoTests"
    "build": "tsc -p tsconfig.json",
    "test": "node scripts/smoke-test.mjs"
    "start:prod": "node dist/main.js",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node --test",
    "audit:offline": "node scripts/audit-offline.mjs"
    "lint": "eslint \"src/**/*.ts\"",
- "test": "jest --passWithNoTests"          // Missing comma
- "build": "tsc -p tsconfig.json",          // Duplicate (removed)
+ "test": "jest --passWithNoTests",         // Fixed: added comma
+ "build": "tsc -p tsconfig.json"           // Duplicate removed
"test": "node scripts/smoke-test.mjs"
"start:prod": "node dist/main.js",
"build": "tsc -p tsconfig.json",
  },
  "dependencies": {
    "@nestjs/common": "^11.1.24",
    "@nestjs/config": "^4.0.4",
    "@nestjs/core": "^11.1.24",
    "@nestjs/jwt": "^11.0.0",
    "@nestjs/platform-express": "^11.1.24",
    "@nestjs/throttler": "^6.5.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10",
    "@nestjs/testing": "^11.1.24",
    "@types/node": "^22.7.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.3"
    "@nestjs/testing": "^10.4.0",
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2"
  }
}
